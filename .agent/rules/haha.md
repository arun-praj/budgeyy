---
trigger: always_on
---

Do not run build every time only when user request to fix a bug. Then run build command and verify the bug.

Do not open the browser to verify the result unless a user gives you command